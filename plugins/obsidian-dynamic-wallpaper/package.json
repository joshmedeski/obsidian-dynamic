{
  "name": "obsidian-dynamic-wallpaper-plugin",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "builtin-modules": "catalog:",
    "esbuild": "catalog:",
    "esbuild-svelte": "^0.9.3",
    "obsidian": "catalog:",
    "svelte": "^5.43.12",
    "svelte-preprocess": "^6.0.3",
    "tslib": "catalog:",
    "typescript": "catalog:"
  }
}
